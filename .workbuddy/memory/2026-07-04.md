# 2026-07-04 工作日志

## AI 智能客服系统 - 环境检查

对 `E:\Z_myfile\ai_customer_service` 项目做了启动前环境检查。

### 已就绪
- Python 3.13.12 (workbuddy managed: `C:\Users\hufabing\.workbuddy\binaries\python\versions\3.13.12\python.exe`)
- Node.js v22.22.2 + npm 10.9.7
- MySQL 8.0.31 服务运行中 (mysqld.exe, :3306 LISTENING, 服务名 MySQL)
- Redis 运行中 (redis-server.exe, :6379 LISTENING, 但 redis-cli 不在 PATH)
- 前端依赖已装: `frontend/node_modules` + vite 5.4.21
- 项目端口 8000-8004 / 5173 均空闲

### 待处理
- `backend/.env` 文件缺失（需创建，含 MYSQL_PASSWORD / DASHSCOPE_API_KEY / JWT_SECRET_KEY）
- 后端 Python 依赖完全未安装（managed python 环境是干净的，12 项 MISS：fastapi/uvicorn/aiomysql/redis/jwt/bcrypt/multipart/pydantic_settings/chromadb/dashscope）
- MySQL root 密码非空（空密码连接被拒 `Access denied for user 'root'@'localhost'`），需向用户索取密码
- 数据库 `ai_customer_service` 是否已建待确认（需密码登录后核查）

### 关键发现
- `backend/common/config.py:73` 的 `env_file=".env"` 是相对路径，.env 必须放在运行命令的工作目录（即 `backend/` 下）
- 历史日志 `backend/logs/app.log` 显示 2026-07-03 曾跑过，DashScope 适配器创建成功、RAG 检索过，但遇到 "query 向量化超时" —— 疑网络或 API Key 额度问题
- `backend/scripts/` 下是 `init_db.py` 和 `init_admin.py`（平铺，不是 README 说的 `scripts/init/`）
- Docker 未安装（如需容器化部署要另装）

### 启动顺序建议
1. 用户提供 MySQL root 密码 + DashScope API Key
2. 创建 `backend/.env`
3. 在 managed python 装依赖：`pip install -r requirements.txt`
4. `python scripts/init_db.py` + `python scripts/init_admin.py`
5. 启动 5 个后端服务（8001→8004→8000）
6. `cd frontend && npm run dev`（前端依赖已就绪）
