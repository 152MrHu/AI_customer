# 2026-07-05 工作日志

## AI 智能客服系统 - 全栈启动成功

### 启动结果
6 个进程全部启动完成，HTTP 200 验证通过：
- user_service :8001 ✅
- chat_service :8002 ✅
- knowledge_service :8003 ✅
- ai_service :8004 ✅
- gateway :8000 ✅ (/docs 返回 200)
- frontend :5173 ✅ (Vite 5.4.21, 页面返回 200)

### 启动命令（每个服务一个终端，都要先 cd 到 backend/）
所有后端命令的执行目录必须是 `E:\Z_myfile\ai_customer_service\backend`（两个原因：模块查找 + .env 相对路径）

```
cd /d E:\Z_myfile\ai_customer_service\backend
E:\Z_myfile\ai_customer_service\.venv\Scripts\python.exe -m user_service.main      # :8001
E:\Z_myfile\ai_customer_service\.venv\Scripts\python.exe -m chat_service.main      # :8002
E:\Z_myfile\ai_customer_service\.venv\Scripts\python.exe -m knowledge_service.main # :8003
E:\Z_myfile\ai_customer_service\.venv\Scripts\python.exe -m ai_service.main        # :8004
E:\Z_myfile\ai_customer_service\.venv\Scripts\python.exe -m gateway.main           # :8000
```
前端：
```
cd /d E:\Z_myfile\ai_customer_service\frontend
npm run dev   # :5173
```

### 本次踩的坑
1. **ModuleNotFoundError: No module named 'user_service'** —— 在 `C:\Users\hufabing\` 下直接跑 `-m user_service.main`，没 cd 到 backend/。Python 找不到模块。
2. **PermissionError [WinError 32] 日志文件占用** —— `user_service/main.py` 的 `reload=True` 会起父子双进程，跨零点时 `TimedRotatingFileHandler`（`common/logging_config.py:30`）触发按天轮转，子进程 rename app.log 时父进程还握着句柄 → Windows 不让 rename。**不影响业务**，服务照常起。彻底解决：要么关 reload，要么换 ConcurrentLogHandler。首次启动（非零点）不会触发。

### 访问入口
- 前端页面：http://localhost:5173/
- API 文档：http://localhost:8000/docs
- 默认账号：admin / admin123，或 testuser / user123

### 待观察
- DashScope 向量化是否还会超时（2026-07-03 遇到过，本次启动 ai_service 时适配器初始化正常，但实际对话时才能验证）

## AI 无法回答问题排查（00:40 - 00:55）

### 问题现象
前端对话报"下游服务不可用"（错误码 5002），AI 不输出任何内容。

### 根因 1：DashScope SDK 兼容性 bug（已修复）
`backend/ai_service/adapter/dashscope_adapter.py:96` 用 `getattr(getattr(response, "output", None), "embeddings", None)` 访问 embeddings，但 dashscope SDK 1.26.2 的 `response.output` 返回的是 **dict** 不是对象，导致 `getattr(dict, "embeddings")` 返回 None → 报"向量化服务返回空结果" → 文档入库失败 → 知识库为空 → AI 返回 5003。
**修复**：改为先判断 `isinstance(output, dict)` 再 `.get("embeddings")`，兼容 dict 和对象两种返回。修复后直接调 ai_service embedding 接口正常（0.5 秒返回，维度 1024）。

### 根因 2：httpx 连接池死连接（已修复，待重启验证）
`backend/common/http_client.py` 的 `httpx.AsyncClient` 默认开 keep-alive，连接池复用连接。当下游服务 reload 重启后（如 ai_service 因代码改动触发热重载），池里旧连接失效，gateway/knowledge_service/chat_service 复用死连接 → `httpx.RequestError`（err 为空）→ 报 5002 "下游服务不可用"。
**修复**：`http_client.py:create_client` 改为 `limits=httpx.Limits(max_keepalive_connections=0)` 禁用 keep-alive，每次请求新建连接。代价是轻微性能损失，但彻底避免死连接问题。

### 排查路径
1. 直接 curl ai_service:8004/api/ai/chat → 返回 5003 "知识库为空"（说明 ai_service 本身正常，问题是知识库没文档）
2. 尝试上传文档 → gateway 超时 30 秒返回 5002
3. 直接 curl ai_service:8004/api/ai/embedding → 0.5 秒正常返回（说明 ai_service 没问题）
4. gateway 日志：`转发请求异常: POST ...8003... err=` （err 为空 = httpx RequestError）
5. 定位：httpx 连接池缓存死连接

### 关键文件
- `backend/ai_service/adapter/dashscope_adapter.py` - DashScope 适配器（修了 output dict 兼容）
- `backend/common/http_client.py` - httpx 客户端单例（修了禁用 keep-alive）
- `backend/gateway/proxy.py` - 网关转发（5002 错误来源）
- `backend/ai_service/services/rag_service.py:62-72` - 知识库为空时返回 5003

## 新建会话无响应排查（09:47 - 10:06）

### 问题现象
前端点击"新建会话"按钮无反应，请求 hang 死。chat_service 认证过的请求全部超时 30 秒，未认证的 401 请求正常返回。

### 根因：aiomysql 连接池泄漏（已修复）
`common/database.py` 的 `DB` 类有严重 bug：
- `DB.__aenter__`: 先 `pool.acquire()` 拿上下文管理器，再手动 `__aenter__()` 取连接 → 池标记"使用中"
- `DB.__aexit__`: 调用 `conn.close()` 关闭物理连接，但**从不调用 `pool.acquire().__aexit__()`** → 连接永远不释放回池
- 每次用 `DB()` "吞"一个连接，池 maxsize=10，约10次DB操作后池耗尽
- 新请求 `pool.acquire()` 永远等不到连接 → hang 死

### 验证
- MySQL `SHOW PROCESSLIST` 只有 2 个连接（物理连接已被 close() 关掉）
- 直接 httpx 发带 x-user-id 的请求 → ReadTimeout（走DB就hang）
- 直接 httpx 发不带 x-user-id 的请求 → 401 正常（不走DB）
- 刚启动时前几次请求正常（池还有空闲），~10次后开始hang

### 修复
- `DB.__aexit__` 改为调用 `self._pool_ctx.__aexit__()` 正确释放回池（而不是 `conn.close()`）
- 同时 `.env` 中 MySQL/Redis/服务地址从 `localhost` 改为 `127.0.0.1`（避免 Windows localhost 优先解析 IPv6 ::1）

### 需要：重启所有使用 DB 类的服务（user/chat/knowledge）+ gateway

## ChromaDB 多进程锁冲突修复（10:19 - 10:30）

### 问题现象
- "新建会话"已修复（database.py 连接池泄漏）
- 但发送消息后 AI 不回复，前端显示 "AI服务暂时不可用" / "知识库为空，请先上传文档"
- 直连 ai_service `/api/ai/chat` → **20秒超时，0字节响应**（TCP 连接建立但 HTTP 无响应）
- chat_service 日志：`httpx.ReadError` — 从 ai_service SSE 流读取时连接中断
- 外部 Python 进程访问 ChromaDB PersistentClient → **Segmentation fault**
- 根因：**ai_service 和 knowledge_service 同时用 PersistentClient 访问同一 chroma_data 目录** → ChromaDB 内部 SQLite/WAL 文件锁冲突 → ai_service 在 `count_collection()` 或 `query()` 时 hang 死

### 修复方案：ChromaDB 单进程访问 + 远程 API 调用
1. **knowledge_service/routers/vector_search.py 新建**：新增两个内部接口：
   - `POST /api/knowledge/search` — 向量检索（kb_id + query_embedding + top_k）
   - `POST /api/knowledge/count` — 查询 collection 向量数量
2. **ai_service/main.py**：移除 `import chromadb` 和 `PersistentClient` 初始化，改为初始化 httpx 客户端
3. **ai_service/dependencies.py**：移除 `_chroma_client`/`set_chroma_client`/`get_chroma_client`
4. **ai_service/retrieval/vector_store.py 重写**：从本地 ChromaDB 操作改为 async HTTP 远程调用 knowledge_service
5. **ai_service/services/rag_service.py**：`count_collection` 和 `query` 都改为 `await` 异步调用；移除 `chroma_client` 参数
6. **ai_service/routers/chat.py**：移除 `chroma_client=Depends(get_chroma_client)`

## 知识库为空时允许纯对话（10:29）

### 需求
用户希望知识库没有文档时也能和 AI 聊天（不报错）。

### 改动
1. **rag_service.py**：`count == 0` 时不再返回错误，改为跳过检索直接对话
2. **prompt.py**：三种模板 SYSTEM_PROMPT_KB（严格）/ SYSTEM_PROMPT_ASSISTANT（联网搜索）

## 前端 FormData Content-Type 修复（12:03）

### 修复
前端 request.js 检测 FormData时不设 Content-Type（让浏览器自动设 multipart/form-data）

## 双模式对话功能（12:46）

### 改动清单
1. 数据库：sessions 表新增 `mode VARCHAR(20) DEFAULT 'kb'`
2. chat_service：schema/session/service/repo/client 全链路增加 mode 参数
3. ai_service：RagChatRequest 增加 mode；rag_service 双模式分支（kb=严格RAG，assistant=LLM+搜索）
4. ai_service/adapter/dashscope_adapter.py：chat_stream 支持 enable_search，搜索模式用 qwen-plus + messages 格式
5. 前端：SessionList 增加模式选择器（Segmented），Chat.jsx 显示当前模式标签
